import { Skeleton } from '@/components/ui/skeleton';

const SkeletonImageDetailProduct = () => {
    return <Skeleton className="order-first h-[416px] w-full rounded-2xl lg:order-last" />;
};

export default SkeletonImageDetailProduct;
